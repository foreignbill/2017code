#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
	int T;
	ll n,a,m,b;
	cin>>T;
	while(T--){
		scanf("%lld%lld%lld%lld",&n,&a,&m,&b);
		if(n==1 && m==1 || (n==m && a==b))
			puts("=");
		else{
			int t = rand() % 2;
		}
	}
	return 0;
} 
